<a href="{{ app.request.getBaseURL() ~ '/blog/show/' ~ post.id}}" style="text-decoration:none; color:black;">
	<div class="col-lg-12 my-5 shadow-lg" style="background-color:white;height:700px;border-radius:25px;">
		<div class="col-lg-12" style="background: url({{ post.img_url }});height:60%;background-size: cover;border-radius: 25px 25px 0 0;  background-position: center;"></div>
		<div class="col-lg-12 px-5">
			<div class="col-lg-12 py-4">
				<h2>{{ post.title }}</h2>
				<h5 style="color:grey;">{{ post.chapô }}</h5>
			</div>
			<p>{{ post.created_at|date("F jS Y") }}</p>
		</div>
	</div>
</a>
